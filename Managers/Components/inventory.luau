local inventory = {}
local myPresets = require(game.ReplicatedStorage.Modules.Shared.myPresets)

function inventory.GetItem()

end

function inventory.HasItemType(itemType:myPresets.itemTypes)
end

return inventory